function AddButton() {
  return (
    <input
      type="submit"
      value="新增紀錄"
      style={{
        margin: "10px",
        padding: "5px",
        color: "black",
        fontSize: "20px",
        backgroundColor: "white",
        borderRadius:"10px"
      }}
    />
  );
}

export default AddButton;
